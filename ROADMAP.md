# Personal Memory Stack 推进顺序

## P0 长期记忆基础框架（当前）

- [x] 对象身份、导航集合和父子关系分离
- [x] Profile / State / Timeline 三层模型
- [x] 类型化 Profile 与 State 栏目契约
- [x] Profile 必填字段和模板兼容校验
- [x] 人类视图与机器元数据分层
- [x] Profile + State + Timeline 组合投影端到端测试
- [x] 受管文件登记、更新、迁移、回滚集成验收

## P1 首个正式对象验收

- [x] 为“AI 外置记忆”整理基本档案并标记为试运行
- [x] 合并当前状态和演进记录
- [x] 生成评审预览并通过结构与人类视图门禁
- [x] 用户允许按当前版本继续后进入正式长期记忆目录试运行
- [x] 验证重复发布无副作用，人工修改时拒绝覆盖

## P1.5 日报生成与运行治理

- [x] Daily V2 证据分类、编排和质量门禁
- [x] 历史日期增量回填与缓存续跑
- [x] 智谱调用参数、Markdown 结构识别和有界修复
- [x] 原始记录 / 日报 / 版本 / 隔离件状态总览
- [x] 建立同一 Speaker、同一智谱 Provider、既定处理目的的持续授权策略

## P1.6 生产闭环与可靠性（当前最高优先级）

- [ ] 安装日常增量任务：同步原始记录、生成缺失 Daily V2、刷新对象候选和状态页
- [x] 追踪异步 Retain operation：按 Speaker 安全区分已受理、处理中、成功和失败
- [ ] 为 Retain 增加持久化幂等键，防止客户端重试再次生成重复 Document
- [ ] 对失败任务执行有界重试；超过上限进入隔离并在状态页告警
- [ ] 状态页补充待处理 operation、缺失日报、隔离件、未晋升 Claim 和过期对象
- [ ] 完成至少一个自然日的无人值守验收，确认无需人工刷新或重复授权

## P1.7 图片与附件证据链

- [ ] 为 MCP / Gateway 定义附件 contract：文件名、MIME、字节数、SHA-256、稳定附件 ID 和说明文字
- [ ] 完成 ChatGPT 图片到 Hindsight Attachment（或独立对象存储）的 A07 端到端验收
- [ ] 原图、OCR / 视觉理解和用户说明分层保存，派生分析不得替代原图
- [ ] Obsidian 投影同步附件并生成稳定 `![[...]]` 引用
- [ ] 验证重复上传、记录修正、删除、孤儿清理、备份与恢复

## P1.8 身份与记忆归属模型（重要、不阻塞当前生产闭环）

- [x] 冻结 Current Speaker、记录 Speaker、Subject / Participant、Scope 的语义边界
- [ ] Gateway 返回结构化归属信息，不依赖客户端从自然语言猜测
- [ ] 只有记忆主体与 Current Speaker 一致时才允许转换为第二人称“你”
- [ ] household / shared 事实允许使用“你们家”，但个人行为必须保留行为主体
- [ ] 主体不确定时使用中性表达，并进入归属待确认项
- [ ] 增加 Monica / liangzai 跨 Speaker 串线回归测试

## P2 持续更新机制（下一主线）

- [x] 多日 Evidence 到已确认对象的候选关联（零 LLM，去重与冲突门禁）
- [x] 候选到 Claim 的相关性精炼与证据 facet 拆分（草稿评审层）
- [ ] 按风险策略审核当前 88 条 Claim：项目类允许自动晋升候选，健康、交易和冲突项要求更严格门禁
- [ ] Claim 到 Subject State 的聚合、冲突检测与晋升门禁
- [ ] 状态新增、替换、解决、重开
- [ ] 冲突与高影响变更评审
- [ ] 原始记录修正后的失效与重算
- [ ] 先完成“AI 外置记忆”从 Evidence 到 Profile / State / Timeline 的全链路发布与幂等复跑验收

## P3 已登记对象的落地顺序

- [ ] AI 外置记忆：项目类，作为低风险首个完整样板
- [ ] 小红书店铺：项目类，验证同类型模板复用
- [ ] 睡眠与精力：健康主题，启用高影响内容评审策略
- [ ] 加密交易节奏：高风险主题，不自动生成行动建议或覆盖用户判断
- [ ] 宠物：先做对象发现与身份确认，再决定独立对象或集合结构
- [ ] 人物、账户、资产：等隐私、身份消歧和权限策略成熟后再扩展

## P4 周期回顾

- [ ] 周报：在日报自动化和 State 更新稳定后优先落地
- [ ] 周期内对象变化摘要，并引用对应 Claim / Evidence
- [ ] 日报、周期回顾和长期对象之间的双向引用关系
- [ ] 月报：积累足够稳定周报后再落地，避免重新从原始记录重复推理

## 已完成里程碑：异步 Retain 状态闭环

- 已核实生产环境 Hindsight `0.10.0` 的 operation 状态接口；状态包含 `pending`、`processing`、`completed`、`failed`、`cancelled` 和 `not_found`。
- `memory-gateway` 新增按 Speaker 隔离的 operation 查询接口，只返回状态、进度、重试和时间信息，不泄露任务原文或内部 payload。
- `memory-mcp` 新增 `memory_operation_get`，客户端可区分“异步已受理”和“最终写入成功”，不再把超时或已受理误报为完成。
- 最终 `status` 是成功与否的权威字段；任务重试后成功时，`last_error` 可能仍保留历史失败，供诊断使用，不能据此覆盖最终状态。
- 已部署并验收提交：`memory-gateway@298abb2`、`memory-mcp@178792e`；正确 Speaker 可查询，错误 Speaker 返回 `404`。
- 本里程碑只解决“已知 operation ID 的安全查询”；operation 自动汇总、失败告警和持久化幂等仍按 P1.6 继续实施。

## 最近执行顺序

1. 补齐异步 Retain 状态追踪、持久化幂等和状态页告警。
2. 安装日常增量任务，并用 2026-09-21 的真实数据完成首轮无人值守验收。
3. 打通一张真实交易截图的附件保存、取回和 Obsidian 展示，确定多模态证据 contract。
4. 在 Claim / State 落地前接入身份归属字段和表达校验，避免后续数据迁移。
5. 对现有 88 条 Claim 做策略化质量门禁，不要求用户逐条人工审核。
6. 实现 Claim 到 State 候选的确定性聚合与证据引用。
7. 实现冲突、替换、解决、重开和源修正后的失效重算。
8. 正式发布“AI 外置记忆”完整对象，并验证重复运行无副作用。
9. 依次落地小红书、睡眠、交易对象；不同风险类型使用不同门禁。
10. 在上述链路稳定后生成第一份周报及对象变化摘要。

## 暂不推进

- 不批量创建未经验证的对象页面；
- 不在日报自动化和对象 State 链路稳定前扩建月报；
- 不把评审样例当成正式成品；
- 不为同一 Speaker、Provider 和既定用途反复请求授权；范围、服务商、敏感级别或用途变化时才重新确认；
- 不把“异步已受理”误报为“已经成功写入”。
