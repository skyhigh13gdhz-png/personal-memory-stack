# 持续脉络 V1 产品规范

> 状态：候选证据层实现中；不自动更新 Subject 当前状态，不写入正式 Obsidian
>
> 优先级：日报 V2 之后，周报/月报之前

## 1. 产品目的

持续脉络回答“一个长期对象如何变化”，不是把所有提到它的日记机械拼接，也不是另一套时间报表。它与日/周/月回顾正交：

- 时间视图以日期为入口，回答某段时间发生了什么；
- 持续脉络以 Subject 为入口，回答项目、健康跟踪或策略如何演进；
- 两者都读取证据，不互相充当事实源。

## 2. 四层模型

```text
Evidence Unit
  → Continuity Candidate（与 Subject 有关联，待判断是否重要）
  → Claim / Change（有证据、类型和有效时间，经过校验）
  → Subject Projection（当前状态、里程碑、时间线、未决问题）
```

V1 只自动完成第一步。候选层允许“多一点”，权威状态层宁缺毋滥。

## 3. Subject 档案结构

```markdown
# Subject 名称
## 当前状态
## 近期进展
## 里程碑
## 决策与承诺
## 未决问题
## 演进时间线
## 不确定与冲突
```

默认人类视图不展示机器 ID；独立审计视图保留 Evidence Unit、Document、字符区间和生成版本。

## 4. 更新规则

| 内容 | 自动候选 | 自动写当前状态 |
|---|---:|---:|
| 项目动作/进展 | 是 | 否 |
| 明确决策、承诺、里程碑 | 是 | 否，先校验 Claim |
| 睡眠、精力、健康单次记录 | 是 | 否 |
| 多日重复模式 | 可生成 observation 候选 | 否，需多日证据与不确定性 |
| 交易与资产状态 | 是 | 禁止；高影响，需人工确认 |
| 推测出的性格、偏好、因果 | 否 | 禁止 |

删除或纠正源证据后，关联候选和投影必须标记 stale，不允许保留为无来源结论。

## 5. V1 机器门禁

1. 只接受配置中已确认的 Subject；
2. 每个候选必须逐条引用现有 Evidence Unit；
3. 每个 Evidence Unit 的原文、日期、Document ID 和字符区间原样保留；
4. 一个单元可关联多个 Subject，但不得因此复制成多个“事实”；
5. 候选默认 `pending`，且 `state_update_allowed=false`；
6. 无 Subject 的 Evidence Unit 不强行归档；
7. 候选包为私有中间产物，不直接同步 Obsidian。

候选评审页展示原始 Evidence Unit，而不是分类器摘要，避免书面化摘要被误当成事实。复合单元与 Subject 的关联只表示“其中存在相关内容”；进入 Claim 层时仍需按原文 facet 拆分和逐字证据校验。

## 6. 下一阶段

1. 用 2026-09-20 已分类数据生成三个已确认 Subject 的候选包；
2. 对候选做人工样例标注：进展、状态、决策、承诺、指标、噪声；
3. 建立 Claim 校验器和高影响分流；
4. 只为“AI 外置记忆”生成第一份持续脉络黄金样例；
5. 黄金样例通过后，再评估是否调用智谱生成候选 Claim；
6. 睡眠与交易 Subject 分别采用更严格的健康/高影响规则。
