# 长期记忆对象基本档案规范

## 1. 页面组成

每个长期记忆对象由三层组成：

```text
Profile   相对稳定的基本档案：是什么、用途、由来、边界
State     随证据变化的当前状态：阶段、重点、问题、待确认项
Timeline  不覆盖的演进记录：对象如何走到当前状态
```

Profile 不能从最新一条事件临时推断，也不能被日报摘要覆盖。State 不能冒充基本定义，Timeline 不能代替 Current State。

## 2. 类型差异

模板同时定义 `profile_sections` 与 `state_sections`：

- 项目：是什么、用途、由来、范围、目标、完成标准；
- 健康主题：跟踪什么、为什么跟踪、关注信号、解释边界；
- 人物：与我的关系、基本情况、记录边界；
- 宠物：它是谁、基本信息、照护重点；
- 账户/资产：是什么、用途、风险边界；
- 策略：策略说明、解决的问题、核心原则、风险边界；
- 系统组件：是什么、负责什么、所属关系、系统边界；
- 独立目标：目标、重要性、达成标准。

字段契约放在 `config/memory-layout.json`，新增或调整模板必须先通过配置校验与多类型测试。

## 3. 数据与展示边界

- `subject-profile-v1` 保存基本档案内容，并通过永久 `subject_id` 与 Registry 关联；
- Profile 必须明确标记 `draft` 或 `confirmed`；草案页面可预览，但不能进入正式长期记忆目录；
- Obsidian 人类视图只显示自然语言章节；机器身份放在隐藏注释或独立 Registry；
- Profile 的必填字段不全时拒绝正式投影，不用空标题或占位符凑页面；
- 高影响类型的派生状态仍需人工确认，Profile 合法不代表 State 可以自动改写。

## 4. 更新策略

- 改名、移动目录：不改变 Profile 身份；
- 基本定位或用途发生变化：修改 Profile，并记录变更原因；
- 日常进展、指标、问题变化：进入 State/Timeline，不频繁改 Profile；
- 对象失去持续维护价值：归档对象，不删除证据和历史。
