# 人类视图与机器元数据契约

## 1. 原则

Obsidian 正文首先服务于阅读、回顾和决策。机器治理字段不能因为实现方便而占用人的注意力。

## 2. 三层信息

| 层级 | 示例 | 存放位置 |
|---|---|---|
| 人类正文 | 发生了什么、当前状态、分析、下一步 | Markdown 标题与正文 |
| 人类有用状态 | 更新至某日、待确认、已归档 | 正文中的自然语言；确有查询需求时才使用属性 |
| 机器治理 | `subject_id`、模板版本、哈希、Evidence ID、投影版本 | HTML 注释、Registry、Manifest 或 JSON 审计产物 |

## 3. 默认展示规则

- 新的人类页面默认不生成 YAML Frontmatter；
- 不在可见正文展示 UUID、哈希、`subject_id`、Claim/Evidence/State ID 或模板版本；
- 不重复展示“事实概览”和同一组分类正文；
- 正式日报不显示“生成审计”“证据索引”“来源 UUID”；这些内容进入独立审计产物；
- 不确定性对人有用，应写成“待确认”“现有记录不足以判断”等自然语言；
- 日期只有表达内容有效期或更新时间时才显示，不用缺省时间伪造事件时间。

## 4. 例外

- 系统调试页可以显示审计信息，但文件名和页首必须明确标记“审计”或“评审”；
- 即便是评审页，机器字段也默认隐藏；只有用户确实需要检查实现细节时才生成独立技术审计页；
- 历史 V1 文件不自动覆盖，避免破坏人工编辑；新版生成器不得继续复制其展示问题。

## 5. 自动化门禁

人类视图测试至少检查：

- 不以 YAML 分隔符开头；
- 去掉 HTML 注释后，不出现机器身份字段；
- 默认模式没有可见证据索引和生成审计；
- 审计模式与人类模式必须显式分离。
