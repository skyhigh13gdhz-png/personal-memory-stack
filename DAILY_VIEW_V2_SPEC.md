# 日回顾 V2 产品规范

> 状态：Gate B 私有黄金样例与确定性渲染器已实现；首次自动编排已完成但未通过产品门禁，尚未写入正式 Obsidian 目录
>
> 形成日期：2026-09-20
> 核心原则：日报是给人读的；证据链默认隐藏但必须随时可审计。

## 1. 当前问题

Evidence Unit V1 已解决“不丢信息”，但只完成了单条分类，尚未形成真正的日报编辑能力：

1. 只有一级栏目，没有项目、餐次、训练类型、宠物事件类型等二级组织；
2. 单条摘要各自成立，但缺少同类合并、去重和总分结构；
3. 语言被统一成书面腔，破坏用户原有表达方式；
4. 事实、计算结果和模型分析混在一起，容易制造未经支持的因果；
5. Evidence Unit 状态和证据索引占据正文，审计信息压过阅读内容。

## 2. 日报读取模型

V2 不是继续扩充 `category`，而是在 Evidence Unit 之上增加一个可验证的编辑层：

```text
Evidence Unit（完整原文）
  → Classification（主栏目、可见范围、重要性）
  → Facets（项目、餐次、训练类型、事件类型等多维标签）
  → Grouping（同项目/同餐次/同主题归组）
  → Composition（合并去重、标签化、人类语气）
  → Analysis（可选；与事实严格分层）
  → Human View / Audit View
```

建议的中间字段：

```text
primary_section    sleep | food | exercise | project | trading | pet | leisure | other
group_key          稳定机器键，例如 project:personal-memory、meal:breakfast
group_title        人类标题，例如 AI外置记忆项目、早餐
item_label         VibeCoding、Hindsight依赖、力量训练、健康、美好瞬间
display_text       尽量保留用户措辞的正文
facets[]           可跨维度检索，但不在多个栏目重复展示
evidence_unit_ids[]
```

一个 Evidence Unit 只能有一个 `primary_section`，但可以有多个 `facets`。例如“处理 Hindsight 的同时看盘和交易”主栏目可归项目，同时带 `trading` facet；周报统计交易时仍能找到它，日报正文不重复两遍。

## 3. 用户验收样例

### 3.1 项目与工作

```markdown
## 项目与工作

### 1. AI外置记忆项目
- VibeCoding：跟 Codex 协同看处理 Hindsight 记忆库有什么需要处理。
- Hindsight 依赖：与 GPT 讨论 Hindsight 的 LLM API 接口并注册智谱 AI 接口。

### 2. 小红书店铺项目
- 笔记发布：下午跟老婆集中编辑小红书笔记，2 个账号各完成 5 篇笔记。
```

### 3.2 饮食与消费

```markdown
## 饮食与消费
- 早餐：起床后喝热水并吃了两片面包。
- 午餐：跟老婆去木桶饭家吃炒菜，点了红薯叶炒回锅肉和酸辣土豆丝。
- 晚餐：煮了粥，煎了手抓饼和两个鸡蛋。
```

### 3.3 运动与宠物

```markdown
## 运动
- 力量训练：下午完成 3 组俯卧撑，每组 10 个。

## 宠物
- 健康：晚上洗澡后跟老婆一起给家里的三只猫刷牙。
- 美好瞬间：做俯卧撑时小黑跑到身体下面做拉伸动作，很可爱。
```

### 3.4 睡眠

```markdown
## 睡眠

### 夜间睡眠
- 入睡：凌晨 1 点左右。
- 起床：早上 8 点左右，起床时头脑昏沉。
- 时长：约 7 小时（程序计算）。

### 午间休息
- 时间记录：下午 2 点半后睡了半小时。
- 感受：醒来仍感困倦。
```

没有记录的“感受”或“分析”不生成空项目。分析必须独立于事实：睡眠时长可由程序计算；“追剧导致晚睡”没有直接证据，只能写成标记为推测的“当天有追剧和晚睡记录，是否存在因果尚不能确认”，不能冒充事实。

## 4. 语言规范

- 保留用户稳定用词：`老婆` 不擅自改成 `妻子`；
- 保留领域表达：`推保护`、`打保护离场` 不机械改写成“设置保护”；
- 避免“进行、食用、共进、相关事务”等报告腔；
- 只修正明显错字和语音识别错误，不把私人日记改成公文；
- `display_text` 可合并重复，但不能增加原文没有的结果、动机或因果。

交易样例：

```markdown
- 合约交易：山寨多单做了推保护处理，不过市场走势不如预期，打保护离场。
```

## 5. 人类视图与审计视图

默认人类视图：

- 不显示 “Evidence Unit 预览”；
- 不显示可见的“证据索引”；
- 证据定位以 Markdown HTML 注释保留，Obsidian 阅读模式不可见；
- 用户看到的只有日期、栏目、归组和内容。

审计视图通过显式 `--audit-details` 生成，显示分类覆盖、fallback 状态和完整证据脚注。审计视图只用于调试、验收和纠错，不同步到正式日报目录。

## 6. 质量门槛

1. 100% Evidence Units 保留；
2. 事实条目 100% 能定位原文；
3. 同一事实不跨栏目重复展示；
4. 项目类内容必须按具体项目归组，不能全部堆在“项目与工作”；
5. 饮食、运动、睡眠和宠物使用稳定、可读的 `item_label`；
6. 语言风格不得无故书面化；
7. 计算、推断和事实在数据结构与页面上明确分层；
8. 默认页面不得暴露机器 ID、hash、Document ID 或审计统计。
9. 一级栏目必须使用固定 `section_id`，模型不得自行创建同义栏目；
10. 项目事实必须统一位于“项目与工作”，并按具体项目形成二级分组；
11. 原始事实、程序计算、跨单元观察和推断必须显式区分；单证据条目不得标为计算结果；
12. 每个 Evidence Unit 必须至少出现在分类对应的主栏目，跨 facet 展示不能替代主栏目落位。

## 7. 下一实现顺序

1. 用首次真实响应回归验证固定栏目、项目归组和事实/分析门禁；
2. 保留人工 V2 黄金样例作为自动结果的阅读基线；
3. 经用户另行授权后只重试一次 Provider，比较结构、语气、证据覆盖和 token；
4. 自动结果通过机器门禁后再进行人工阅读验收；
5. 用户确认阅读价值后，才写入正式 `10-时间视图/日`。
